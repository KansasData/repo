
```{=html}
<ul class="p-list list">
  <% for (const item of items) { %>
    <li <%= metadataAttrs(item) %>>
      <span class="listing-title lead"><%= item.title %></span><br />
      <span class="listing-partnership fst-italic"><%= item.partnership %></span>
      <span class="listing-year">(<%= item.year %>)</span><br />
  
      <!-- Two-column Partner | License row -->
      <div class="d-flex justify-content-between align-items-center quarto-title-meta">
        <span class="listing-partnership fst-italic"><%= item.partnership %></span>
      </div>
  
      <!-- Buttons in specified order: Details, License, Dataset, Code -->
      <a href="<%- item.path %>" class="btn btn-primary">Details</a>
      <% if (item.license && item.licenseType) { %>
        <a href="<%- item.license %>" class="btn btn-dark fst-italic" target="_blank" rel="noopener noreferrer">
          <%= item.licenseType %>
        </a>
      <% } %>
      <% if (item.dataset) { %>
        <a href="<%- item.dataset %>" class="btn btn-light" target="_blank" rel="noopener noreferrer">
          <i class="bi bi-file-earmark-zip"></i> Dataset
        </a>
      <% } %>
      <% if (item.code) { %>
        <a href="<%- item.code %>" class="btn btn-light" target="_blank" rel="noopener noreferrer">
          <i class="bi bi-file-earmark-code-fill"></i> Code
        </a>
      <% } %>
    </li>
  <% } %>
  </ul>
  

```
